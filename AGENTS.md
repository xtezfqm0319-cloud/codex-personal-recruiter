# Codex 个人招聘工作台长期规则

## 定位与事实来源

- 本项目只服务当前用户和当前公司，是本地个人招聘主档案，不是通用 SaaS。
- Codex 对话是操作入口；本地文件是正式产物。Markdown 主档案是唯一事实来源，CSV 仅是可重建索引，JSONL 仅是审计记录。
- 开始任务前先读取现有公司、岗位、候选人及原始材料；重要结论必须落盘。

## 工作原则

- 模型负责理解、质疑、判断和生成；脚本负责扫描、提取、哈希、移动、重命名、索引和校验。
- 不猜测缺失事实。判断必须引用文件和材料证据，并区分事实、判断、未验证项。
- 原始简历和面试纪要不可被生成内容覆盖；保留来源路径和 SHA-256。
- AI 建议与人工正式结论必须分开；不得用重写报告覆盖人工结论。
- 简历只使用“强推、推、建议待定、建议淘汰”四档，不使用精确评分。
- 公司学历基线见 `00_公司认知/通用招聘标准.md`；破格必须记录具体理由。

## 个人化与交互

- 所有岗位、简历、面试、终面和历史人才判断，都先读取 `00_公司认知/个人招聘判断偏好.md`。已确认偏好可影响判断；待确认候选不得作为正式标准。
- 用户用自然语言表达目标即可。例行任务直接执行，默认只汇报结果、业务优先级、关键依据和需要用户决定的事项；除非失败或用户询问，不展开 CLI、移动、索引等内部过程。
- 多名候选人默认给岗位内相对比较，不只逐份复述。排序必须解释相邻候选人的关键差异，不使用精确评分。
- 面试前优先生成针对候选人的验证问题；面试后说明验证了什么、推翻了什么、还缺什么，以及是否值得继续投入下一轮。
- 终面简报必须给出清晰倾向和可能改变倾向的关键未验证项，同时保留“人工最终决定”边界。
- 只有用户明确表达并确认的招聘偏好才能进入正式偏好区。单个决定或一次分歧只能形成待确认候选，不得自动泛化。
- 重要业务视图落盘：今日简报写入 `04_全局索引/今日招聘简报.md`；岗位比较写入岗位目录；面试准备写入对应候选人轮次目录。

## 权限

可直接执行：整理文件、分析材料、生成报告、创建候选人档案、重建索引、将明确匹配的待处理文件移动到确定位置。

以下动作必须先写入 `04_全局索引/待确认事项.md`，获得用户明确确认后再执行，并记录到 `07_运行记录/系统变更记录.md`：

- 删除原始文件；
- 修改正式岗位画像；
- 修改已人工确认的候选人结论；
- 跨岗位移动候选人；
- 修改公司级长期认知；
- 因新版简历改变已确认结论。

## 数据安全与边界

- 不把真实候选人材料提交到公开仓库，不在日志重复记录身份证号、住址等无关敏感信息。
- 不开发或调用外部模型 API；不实现网页、服务、数据库、多人协同、自动寻访/邀约/邮件/Offer。
- 历史人才搜索只给建议，不自动把候选人加入新岗位。
- 岗位校准只生成建议；未经确认不得改正式画像。

## 执行约定

- 优先使用 `.agents/skills/` 中匹配任务的 Skill。
- Python 命令统一从项目根目录运行：`python -m recruiter --root . <command>`。
- 每次有状态变化后重建索引并运行相关校验；错误不得静默忽略。
