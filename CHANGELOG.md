# 更新记录

## v1.0.0｜2026-07-23

### 首个正式版本：招聘工作台总入口与完整招聘闭环

- 新增 `run-recruiting-workbench`，作为模糊、跨环节、连续和全局招聘请求的默认对话入口；明确单环节请求仍可直接进入专业 Skill。
- 总入口只负责理解目标、读取当前状态、路由专业 Skill、连续执行和统一交付，不重新定义简历、面试、终面或岗位判断标准。
- 建立完整意图路由表，覆盖建岗位、简历筛选、候选人比较、人工确认、面试准备与分析、终面、归档、历史人才、岗位校准、个人偏好和今日简报。
- 支持将“处理新材料并告诉我先约谁”“分析一面，值得的话准备二面”等自然语言请求拆成最短完整行动链，一次完成无需用户介入的步骤。
- 增加连续上下文规则：“前两个”“他们”“按刚才建议执行”等表达只绑定最近一次明确且冻结的名单，话题切换或存在多个合理对象时进行一次最小澄清。
- 统一对话交付为“结果 → 关键判断 → 只需你决定 → 接下来”；完整证据落盘，但不把 Skill、CLI、索引和文件移动过程暴露给日常用户。
- 待确认和识别异常改为业务问题表达，提供可能选项、影响和可直接回复的最小输入，而不是只报告文件进入待确认区。
- 增加“证据已足够，可以决定 / 倾向明确，只需验证一个关键问题 / 当前不应下结论”三种定性决策成熟度，不使用分数或百分比。
- 更新 `AGENTS.md`、README、Codex / Claude Code 双端入口和路由契约测试；项目现包含 1 个总入口与 12 个专业招聘 Skills。
- 至此，建岗位、简历处理、候选人比较、人工筛选确认、面试准备、面试纪要、终面简报、候选人归档、历史人才搜索、岗位校准、个人偏好和今日简报全部具备中文完整提示词、证据规范、正式落盘和自动化验证，可作为 `v1.0.0` 正式使用。

## v0.12.0｜2026-07-23

### 个人偏好与今日注意力

- 一次完成 `learn-recruiting-preferences` 和 `daily-recruiting-brief` 两套辅助提示词升级，至此 12 个招聘 Skills 的核心工作流均完成中文准确性优化。
- 偏好学习从“记录用户反馈”升级为“保留个案原意 → 六层分类 → 完整规则候选 → 反例和过度泛化检查 → 精确确认 → 应用审计”。
- 明确区分个案决定、岗位专属标准、岗位族偏好、跨岗位招聘偏好、交互偏好和不可学习内容；一次分歧或一个推进结果不得自动泛化。
- 每条正式偏好必须包含适用范围、可观察信号、决策影响、证据要求、例外、反例、来源和不得进一步推出的结论。
- 新增 `propose-preference` 与 `resolve-preference` 受控入口：提案只进入待确认事项，用户确认完全一致的规则后才写入主档案；拒绝规则默认不保留为有效偏好。
- 今日招聘简报从岗位状态汇总升级为跨岗位注意力管理，区分用户决定、AI 可直接继续和外部等待三类行动所有者。
- 优先级改为依据决策阻塞、信息成熟度、真实时间窗口、延后成本、最小下一步和可逆性判断，不按文件年龄、数量或缺失字段制造紧迫感。
- 前三事项允许少于三项；用户决策被压缩成选项、AI 倾向、最强依据、最大风险和最小输入，并为可延后事项写明重新进入优先队列的触发条件。
- 新增 `prepare-daily-brief` 确定性事实底稿、两套详细中文参考规范、两套共享模板、结构校验、自动化测试和端到端演示步骤。

## v0.11.0｜2026-07-22

### 主流程收尾闭环准确性

- 一次完成 `close-candidate`、`search-history` 和 `calibrate-position` 三套主流程提示词升级，均使用中文核心工作流、独立详细判断规范和共享输出模板。
- 结束归档从“最终结果 + 可复用布尔值”升级为“人工正式结果 → 结束原因 → 能力证据与结果边界 → 四级复用判断 → 再次验证动作”。
- 增加 `优先复用`、`有条件复用`、`暂不主动复用`、`不建议复用` 四级复用语义，要求同时记录适用岗位、复用前提、风险、反转条件和未来验证。
- 每次新归档自动生成 `归档摘要.md`，保留已经验证、被削弱、仍未验证和“本次结果不能推出什么”，避免把未录用误写成能力不足，或把录用误写成全部风险消失。
- 历史人才检索改为“自然语言查询意图 → 多组关键词召回 → 回到 Markdown 主档案 → 按当前岗位重判 → 相对排序与最小下一步”；关键词命中不再直接视为匹配证据。
- 历史查询结果增加结束原因、复用等级、适用场景、风险、再次验证和归档摘要路径，并明确旧岗位的强推、淘汰或面试通过不能直接继承到当前岗位。
- 岗位校准从简单 AI/人工对照表升级为完整决策链，分别检查可能的假阳性、假阴性、共同误判、无区分力规则、外部条件和样本不可比问题。
- 每条校准建议必须提供准确原文、修改后的完整文字、支持案例、反例、历史反事实、预期收益、误伤风险、观察和撤回条件；最终结果不能反向证明早期判断必然正确。
- 新增候选人归档摘要、历史人才复用建议和岗位校准建议模板；索引增加结束类别、结束原因、复用等级和复用场景字段。
- 正式岗位画像仍受权限保护：校准只生成建议，未经用户确认不得修改 `岗位.md`、画像版本或人工候选人结论。

## v0.10.0｜2026-07-22

### 终面决策简报准确性

- `generate-final-brief` 从材料汇总升级为“岗位决策证据表 → 历轮判断变化 → 下行风险 → 终面信息价值 → 录用讨论倾向”的决策流程。
- 按正式岗位标准而不是文件顺序组织证据，不再重复简历或逐轮复述面试。
- 将岗位胜任、证据可信度和录用可行性分开判断，避免将能力风险、证据缺口和到岗/预算约束混为一个模糊结论。
- 对同一项目在简历和多轮面试中的重复叙述去重，只有新事实、决策、结果口径、反例或独立来源才增加证据量。
- 明确“谨慎推进”与“继续验证”的边界：前者是证据已足够但需接受风险，后者是仍有关键证据阻塞录用判断。
- 增加终面是否值得的信息价值判断，删除历轮已充分回答的重复问题。
- 每个终面问题必须对应录用阻塞项、风险或冲突，并包含追问、强弱证据、反证和倾向变化条件。
- 更新终面决策简报模板、生成骨架、结构校验、Codex/Claude Code 双端 Skill 和提示词契约测试。

## v0.9.0｜2026-07-22

### 面试纪要分析准确性

- `analyze-interview` 从简短的纪要整理升级为“问题覆盖 → 证据单元 → 岗位映射 → 本轮变化账本 → 多轮累积 → 下一步倾向”的完整分析流程。
- 严格区分面试官评价、候选人陈述、纪要中的证据事实、AI 独立分析和人工正式结论，避免把评价或候选人主张直接写成事实。
- 对照本轮面试准备逐项标记充分回答、部分回答、未回答、未提问和计划外新证据，明确问过不等于答到了。
- 增加得到支持、被削弱、被推翻和保持不变的变化账本；没有新证据时不制造“再次验证通过”。
- 多轮分析对同一项目和重复评价进行证据去重，后续更具体证据可以增强、限制或推翻早期判断。
- 材料矛盾先检查时间、范围和统计口径差异；可信度风险必须引用具体变化或回避行为，不使用“感觉像包装”。
- AI 下一步倾向统一为建议推进、继续验证、建议暂缓和不建议推进，并必须说明反转条件及再安排一轮是否值得。
- CLI 新增问题覆盖、判断变化、矛盾、倾向、下一轮验证价值和个人偏好影响等可选落盘字段。
- 人工面试结论支持记录明确理由；修改已有结论时先生成待确认事项，再通过目标结论完全匹配的二次确认入口执行。
- 更新面试报告模板、候选人多轮摘要、兼容校验、Codex/Claude Code 双端 Skill 和提示词契约测试。

## v0.8.0｜2026-07-22

### 面试准备准确性

- `prepare-interview` 从简短的问题生成说明升级为“本轮决策任务 → 证据地图 → 高信息价值问题 → 追问分支 → 判断变化条件”的完整面试设计流程。
- 每轮先明确结束后需要支持什么决定、当前倾向是什么，以及哪一个未验证问题最可能改变倾向，不再以“全面了解候选人”为目标。
- 将岗位判断项分为已有强证据、只有主张、材料冲突、关键未验证和本轮不处理，避免重复询问已经充分确认的内容。
- 每道题必须关联正式岗位标准和候选人具体材料，并包含模糊回答、个人贡献、决策取舍、结果归因及反例的追问路径。
- 强弱信号改为可观察的任务、行动、决策、约束、结果和边界证据，不把表达流畅、紧张程度或主观气场当作能力。
- 根据 30、45—60、60—90 分钟面试动态控制主问题数量，并始终给出时间有限时必问三题。
- 增加一至五轮的默认深度、历轮证据去重、材料矛盾中性澄清、偏差控制和 25 项写入前自检。
- 更新面试准备模板、Codex/Claude Code 双端 Skill 和提示词契约测试。

## v0.7.0｜2026-07-21

### 人工筛选确认准确性

- `confirm-screening` 从逐人记录命令升级为自然语言批量决定解析，支持姓名、顺序、AI 建议分组、集合例外和明确代词。
- 对“前两个”“第三个”“其余”等表达先冻结当前名单再逐人展开，避免执行过程中列表变化导致指代错位。
- 明确区分正式处理决定、相对评价和资源安排；未被用户覆盖的候选人继续保持待确认，不自动补成待定或淘汰。
- 人工筛选理由可独立写入候选人主档案，并保持用户归因；不从 AI 分析补写理由，也不把材料缺口改写成能力事实。
- AI 建议与人工结论继续使用独立字段，分歧会被保留并在汇报中说明，不通过改写建议制造一致。
- 补齐已有人工结论变更的闭环：首次变更先进入待确认事项，用户再次明确确认后才通过受控入口执行并标记已处理。
- 增加模糊表达、批量范围、淘汰归档、个人偏好边界和 18 项写入前自检规范。
- 更新候选人总览模板、Codex/Claude Code 双端 Skill 和提示词契约测试。

## v0.6.0｜2026-07-21

### 候选人横向比较准确性

- `compare-candidates` 从简短排序说明升级为阶段可比、证据归一、优先组和有限名额取舍模型。
- 不再强制所有候选人形成唯一名次；差异不足时允许有依据的并列，并要求给出打破并列的最小验证动作。
- 区分岗位适配、证据成熟度和当前投入价值，避免把流程阶段更深直接当作能力更强。
- 对重复引用的同一经历进行证据去重，只有新增事实、核验过程或反例才能改变证据强度。
- 增加支配关系、优势冲突、相邻候选人审计、最后入选者与第一未入选者的边界取舍。
- 每个影响资源分配的顺序都必须说明决定性证据，以及什么新证据可能造成反转。
- 更新岗位候选人比较模板、Codex/Claude Code 双端 Skill 和提示词契约测试。

## v0.5.0｜2026-07-21

### 简历判断准确性

- `process-resumes` 从“逐份总结并给四档建议”升级为“岗位证据映射 + 同岗位二次比较”的两遍判断流程。
- 新增完整的简历筛选判断与输出规范，明确材料可判断边界、证据强弱、个人贡献、四档边界、硬性条件例外和待定使用条件。
- 强推必须同时达到岗位绝对标准并在同岗位中相对突出；不设固定比例，也不为排序制造虚假分数。
- 明确区分已确认不匹配、证据缺口、材料矛盾、经历距离、可信度风险和普通未验证，避免把“简历没写”当作候选人不会。
- 业务摘要改为直接支持批量决策，并要求说明最大风险和最可能改变当前结论的新证据。
- 增加针对候选人的后续验证内容与个人偏好影响落盘字段，减少通用追问和重复阅读原简历。
- 公开版本移除特定背景层级和相关淘汰规则，改为由使用者在本地公司标准或正式岗位画像中配置硬性条件。
- 更新简历分析模板、Codex/Claude Code 双端 Skill 和提示词契约测试。

## v0.4.2｜2026-07-21

### 建岗提示词完整版

- 新增 `create-position/references/建岗判断与输出规范.md`，补充第一轮固定回复、决定性问题格式、选人标准分类和证据强弱定义。
- 增加候选人能力冲突时的取舍句式、经历替代规则，以及 AI、沟通、公司背景和专业要求等模糊条件的改写示例。
- 增加用户确认前的完整“选人规则预览”和正式岗位画像格式，降低不同会话之间的输出漂移。
- 增加 15 项写入前自检，防止复述 JD、平铺要求、混淆未验证与不符合，或把背景代理变量当作能力。
- 更新共享岗位模板，使最终主档案能够承接准入、排序、证据和验证规则。
- 增加提示词契约测试，并同步生成 Claude Code 参考文件。

## v0.4.1｜2026-07-21

### 建岗判断准确性

- `create-position` 不再以完善或复述 JD 为主要目标，改为先建立可直接指导简历筛选、候选人排序和面试验证的选人决策模型。
- 第一轮先给岗位理解和初步选人规则，再提问；每轮最多提出三个真正会改变候选人准入、排序、证据要求或面试计划的问题。
- 明确区分一票否决条件、能力底线、决定性排序因素、加分项、风险信号和可放宽条件。
- 增加候选人能力冲突时的取舍规则、经历替代规则，以及强证据、弱证据、未验证项和验证环节定义。
- 降低 JD 宣传性语言和泛化能力词对筛选判断的影响，将其转换为可观察行为、材料证据或验证任务。
- `create-position` 的触发描述、正文和入口示例统一改为中文；后续招聘 Skills 也按中文正文和完整前后对比的方式逐环节优化。
- Codex 与 Claude Code 版本继续从同一 Skill 维护源同步。

## v0.4.0｜2026-07-21

### Codex 与 Claude Code 双端兼容

- 新增 `CLAUDE.md`，Claude Code 会自动导入与 Codex 相同的长期规则和权限边界。
- 新增 `.claude/skills/`，12 个招聘 Skills 可在 Claude Code 中自动匹配或通过斜杠命令调用。
- `.agents/skills/` 保持唯一维护源，通过同步脚本生成 Claude Code 副本并自动转换 Skill 间调用语法。
- 两端共享同一套 Python CLI、Markdown 岗位与候选人主档案、个人招聘偏好和可重建索引。
- 新增一致性测试，防止两端 Skill 名单、参考材料和内容版本漂移。

### 使用成本

- 使用各自支持的订阅账号登录时，项目不需要额外配置模型 API Key，也不会产生项目自己的 API 账单。
- 若主动使用供应商 API Key 登录，相关费用仍以对应供应商规则为准。

### 验证

- Codex 与 Claude Code 两套共 24 个 Skill 入口全部通过结构校验，且同步检查无漂移。
- 14 项自动化测试全部通过。
- 从建岗到归档和历史人才复用的端到端演示通过，演示及当前工作区均为 0 个校验错误。

## v0.3.0｜2026-07-20

### 更懂个人判断

- 新增个人招聘判断偏好主档案。只保存并应用用户明确确认的规则，不从单次决定或沉默中猜测。
- 用户解释一次推荐分歧时，Codex 会区分“这个候选人的个案结论”和“未来可复用的招聘偏好”。
- 已确认偏好会同时影响简历筛选、候选人比较、面试准备和终面简报，并明确说明影响。

### 更直接的日常使用

- 新增“今日招聘简报”，优先告诉用户现在最值得处理的三件事、等待决定的事项和下一步动作。
- 新增同岗位候选人横向比较，直接说明优先顺序、相邻候选人的关键差异，以及面试名额有限时的选择。
- 新增候选人专属面试准备，围绕证据缺口生成主问题、追问和强弱信号，减少重复提问。
- 终面简报使用“建议进入录用讨论 / 谨慎推进 / 继续验证 / 不建议推进”四类明确倾向，并给出决定性理由、最大风险和可能改变判断的新证据。

### 产品边界

- 本次升级以 Skills、长期 Markdown 上下文和输出模板为主，没有增加网页、服务、数据库或外部模型 API。
- 今日简报、候选人比较和面试准备均为可重建辅助视图，正式事实仍以岗位和候选人主档案为准。

### 验证

- 12 个招聘 Skills 全部通过结构校验。
- 11 项自动化测试全部通过。
- 从建岗到归档、历史人才索引和岗位校准的端到端演示复验通过，工作区校验为 0 个错误。

## v0.2.0｜2026-07-17

### 新增

- 增加完全本地的 PDF OCR：默认使用 RapidOCR ONNX，macOS 可回退 Apple Vision，其他环境可回退 Tesseract。
- 增加 PDF 逐页文本完整度检测，覆盖纯扫描 PDF、文本页与扫描页混合 PDF，以及少量文本层叠加图片正文的情况。
- 简历与面试纪要建档后生成独立的文本提取质量报告，记录处理方式、OCR 引擎、逐页有效字符、OCR 使用情况和待确认页。
- OCR 后仍无法完整提取的材料自动进入 `01_待处理/待确认/`，同时生成质量报告和全局待确认事项，不继续自动建档。

### 验证

- 11 项自动化测试通过。
- 项目结构与主档案校验为 0 个错误。
- 真实图片版中文简历 PDF 已实际完成 OCR、姓名与岗位识别、规范化建档和质量报告生成。
- 混合 PDF、少量文本层 PDF 和不可读图片 PDF 均完成针对性烟雾测试。

### 边界

- OCR 和完整度检测不验证候选人材料真实性。
- 手写、严重模糊、复杂表格或非常规排版仍可能进入待确认。
- 不调用外部模型 API，也不会上传候选人原始材料。
