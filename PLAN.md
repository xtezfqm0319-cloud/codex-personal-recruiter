# Codex 个人招聘工作台 V1.0 实施计划

## 基线与边界

- 需求与架构基线：用户确认的 V1.0 需求与架构方案；原始 PDF 已按要求从公开项目移除。
- 形态：单一 Codex 主 Agent、项目 Skills、Markdown 主档案、Python 确定性脚本、可重建索引。
- 不包含：网页、客户端、后端、数据库、外部模型 API、多 Agent、定时任务、ATS、自动寻访/邀约/邮件/Offer/协作。
- AI 判断由 Codex 在对话中按 Skill 完成；Python 不调用模型，只执行文件与状态操作。

## 技术审查

基线没有阻塞性冲突。两个实现约束如下：

1. 项目中未发现独立的“现有终面简报 Prompt”，首版先采用方案中明确的证据、事实/判断/未验证项分离规则，并预留单一参考文件替换点。
2. 自动化测试不验证模型文案逐字一致，而验证输入追溯、结构、状态转换、权限门禁、索引可重建和归档完整性。

## 阶段与验收标准

### 阶段 1：项目底座

- [x] 完整目录、公司认知、模板、YAML 状态格式。
- [x] Python 3.11+ CLI；文件提取、移动、重命名、索引、校验、待确认、审计。
- [x] 基础测试通过；索引可从 Markdown 主档案重建。

验收：`python -m recruiter validate` 无错误；`pytest` 的底座用例通过。

### 阶段 2：建岗位

- [x] `create-position` Skill、待校准岗位、JD/画像模板、历史人才扫描。

验收：从自然语言/JD 建立岗位目录与 `岗位.md`，生成历史人选建议且不自动跨岗移动。

### 阶段 3：简历处理

- [x] PDF/DOCX/TXT/MD 提取、姓名/岗位识别、待确认、标准命名。
- [x] 四档分析、统一排序、批量人工确认、推进建档/淘汰归档。

验收：测试批次完成输入到人工结论落盘，AI建议和人工结论字段独立。

### 阶段 4：面试纪要

- [x] 候选人/岗位/1—5轮识别、原文保存、三层结论、总览更新。

验收：原始纪要未被覆盖，报告可追溯，无法识别文件进入待确认。

### 阶段 5：终面简报

- [x] 汇集岗位、简历、分析、历轮材料、HR补充信息，生成证据化简报。

验收：事实、判断、未验证项明确分区；不替代录用决定。

### 阶段 6：结束归档与复用

- [x] 完整档案归档、原岗位短索引、复用标记、历史查询与建岗扫描。

验收：归档后全局索引有效，历史搜索只读且不自动加入新流程。

### 阶段 7：岗位校准

- [x] 对比画像、AI建议、人工结论、面试/最终结果，生成校准建议。
- [x] 正式岗位画像修改必须进入待确认。

验收：校准只生成建议和差异，不直接改 `岗位.md`。

## 最终验收

- [x] 全部自动化测试通过。
- [x] 实际运行：新建岗位 → 简历处理 → 人工确认 → 面试分析 → 终面简报 → 流程结束 → 归档 → 历史人才索引。
- [x] `README.md` 记录安装、命令、自然语言示例、数据安全和已知限制。
- [x] 演示产物和测试日志保留在项目中。

## V0.3 个人使用体验优化｜2026-07-20

目标：不扩大产品边界，不增加网页、服务或外部 API，只通过长期上下文、Skills 和输出模板，让日常使用更直接、更懂用户、判断更准确。

- [x] 建立个人招聘判断偏好主档案；只应用用户明确确认的规则。
- [x] 增加今日招聘简报，主动聚合最值得关注的事项和最小决策集。
- [x] 增加同岗位候选人横向比较，在四档建议内说明相对顺序和取舍。
- [x] 增加候选人专属面试准备，从证据缺口反推问题、追问和强弱信号。
- [x] 强化简历、面试和终面输出，明确“本轮改变了什么”和推进倾向。
- [x] 完成 Skill 校验、全量测试和端到端演示复验。

验收：用户可以只用自然语言完成“今天先做什么、候选人怎么排、下一轮问什么、最终是否值得推进”四类高频判断；所有结论仍可追溯到本地主档案。

## V0.4 Codex / Claude Code 双端兼容｜2026-07-21

- [x] 新增 `CLAUDE.md` 并复用 `AGENTS.md` 长期规则。
- [x] 生成 Claude Code 原生可发现的 `.claude/skills/`。
- [x] 建立单一 Skill 维护源和双端同步校验。
- [x] 将新生成报告中的助手名称改为中性的“AI”。
- [x] 更新双端安装、使用、订阅登录与显式 Skill 调用说明。
- [x] 运行全部 Skills 校验、自动化测试和端到端演示复验。

验收：Codex 可从 `.agents/skills/` 工作，Claude Code 可从 `.claude/skills/` 工作；两端使用相同本地主档案和 Python CLI，Skill 内容不会静默漂移。

## V0.11 主流程提示词准确性闭环｜2026-07-22

- [x] 完成建岗、简历筛选、候选人比较、人工确认、面试准备、面试分析、终面简报、结束归档、历史人才复用和岗位校准全部主流程提示词优化。
- [x] 结束归档分开人工正式结果、结束原因、能力证据、结果边界和四级复用判断，并自动生成归档摘要。
- [x] 历史查询把关键词仅作为召回，回到 Markdown 主档案后按当前岗位重新判断和排序。
- [x] 岗位校准加入样本可比性、完整决策链、假阳性/假阴性、结果泄漏、反例、历史反事实和误伤风险。
- [x] 三套新增中文详细规范和输出模板同步到 Codex 与 Claude Code。
- [x] 三套 Skill 结构校验通过，双端同步检查通过，31 项自动化测试通过，端到端演示与当前工作区均为 0 个校验错误。

验收：一次招聘从建立岗位到结束归档、历史复用和岗位校准均有明确、可追溯、不会错误泛化的判断规则；正式岗位画像和人工结论仍受原权限边界保护。

## V0.12 个人偏好与今日注意力闭环｜2026-07-23

- [x] 优化 `learn-recruiting-preferences`，明确六层反馈分类、最小适用范围、证据标准、反例检查和精确确认。
- [x] 新增偏好提案与确认 CLI；未确认规则只进入待确认事项，不影响正式招聘判断。
- [x] 优化 `daily-recruiting-brief`，把岗位状态汇总改为跨岗位注意力和最小决策管理。
- [x] 明确用户决定、AI 可直接继续、外部等待三类行动所有者，不把 AI 可完成的工作转嫁给用户。
- [x] 增加真实时间窗口、延后成本、行动价值和重入触发条件，禁止按文件时间制造紧迫感。
- [x] 更新个人偏好与今日简报模板、结构校验、Codex / Claude Code 双端 Skill、自动化测试和端到端演示。

验收：用户纠正一次判断时，系统不会过度学习；用户询问今日重点时，得到的是少量有业务价值、所有者明确、可立即行动的事项，而不是文件和状态清单。

## V1.0 招聘工作台正式版｜2026-07-23

- [x] 新增 `run-recruiting-workbench` 默认对话入口，覆盖模糊、跨环节、连续和全局招聘目标。
- [x] 建立 12 个专业 Skills 的完整意图路由和多环节组合路径，不重复或简化专业判断规则。
- [x] 支持上一轮冻结名单、批量建议和“前两个、他们、按建议执行”等连续指代。
- [x] 明确直接执行、必须停下和可带未验证项继续三类边界，减少无效确认。
- [x] 统一对话结果、关键判断、最小用户决定、判断变化和决策成熟度表达。
- [x] 将待确认、OCR、岗位归属和轮次异常转换为可直接回答的业务问题。
- [x] 接入 `AGENTS.md`、README、Codex / Claude Code 双端同步和自动化契约测试。

验收：用户只需表达招聘目标，不需要识别当前环节或记住 Skill 名称；工作台会连续完成原目标内所有无需用户介入的步骤，并只在真正需要人工决定或关键信息缺失时打断。建岗位到归档、历史复用和岗位校准的完整流程均可在 Codex 与 Claude Code 中使用，满足 `v1.0.0` 正式版标准。

## V1.1 首次招聘判断校准｜2026-08-03

- [x] 扩展 `learn-recruiting-preferences`，加入首次校准触发、逐题对话、针对性追问和反向验证。
- [x] 定义 5 个核心取舍情境：证据门槛、个人贡献、可迁移能力、风险容忍和汇报交互。
- [x] 新增校准工作稿模板和开始、记录、暂停、续接、总结、完成 CLI。
- [x] 将校准结果分为可确认规则、待观察倾向和明确不作推断，并增加效果预演。
- [x] 沿用现有偏好提案和精确确认权限，保证未确认校准结果不影响正式判断。
- [x] 接入招聘总入口；无已确认偏好时只非阻塞邀请一次。
- [x] 补齐结构校验、流程测试、双端 Skill 契约和端到端演示。

验收：用户可在 8–10 分钟内完成 5 个核心情境，全程每次只问一题，可暂停续接；最多 2 个针对性追问和 1 个反向验证；每条规则有范围、证据、影响、例外、反例和禁止推断；只有按编号确认的精确规则进入正式偏好。
